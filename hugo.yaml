baseURL: "https://mohammedfawaz.com/"
languageCode: en
title: "Mohammed Fawaz"

module:
  imports:
    - path: github.com/hugo-toha/toha/v4
  mounts:
    - source: assets
      target: assets
    - source: static
      target: static
    - source: data
      target: data
    - source: layouts
      target: layouts

markup:
  goldmark:
    renderer:
      unsafe: true

params:
  greeting: "Hi, I'm Mohammed Fawaz"
  # This specific nesting tells the theme NOT to use the image-resize function
  background:
    video: "images/hero-bg.mp4"

  footer:
    enable: true
  features:
    theme:
      enable: true
    portfolio:
      enable: true
