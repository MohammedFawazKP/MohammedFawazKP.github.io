baseURL: "https://mohammedfawaz.com/"
languageCode: en
title: "Mohammed Fawaz"

# This is the correct way to tell Hugo to fetch the theme from the cloud
module:
  imports:
    - path: github.com/hugo-toha/toha/v4

params:
  greeting: "Hi, I'm Mohammed Fawaz"
  background:
    video: "images/hero-bg.mp4"

  footer:
    enable: true
  features:
    theme:
      enable: true
    portfolio:
      enable: true
